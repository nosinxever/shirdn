export const metadata = {
    title: "Games 💖",
    description: "Games 💖",
  };


  export default function RootLayout({ children }) {
    return (
      <html lang="en">
        <body >{children}</body>
      </html>
    );
  }
  